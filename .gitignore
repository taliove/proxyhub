# Build artifacts
dist/
/proxyhub
sub-outer/
vendor/

# Embedded GeoIP data (DB-IP Lite country mmdb): downloaded/staged by
# `make geoip-update`, never committed. The .sha256 sidecars in scripts/geoip
# ARE committed (small text, pin the expected checksum).
.geoip-cache/
internal/geoip/*.mmdb

# Frontend build output (vite emits into cmd/server/web; only the
# placeholder index.html is tracked)
cmd/server/web/assets/
node_modules/
web/dist/

# Runtime data
*.db
*.db-journal
*.db-wal
*.db-shm
.test/data/
.test/logs/
.test/tmp/

# Local runtime state (database, logs, generated xray config, pid)
var/

# AI tooling session state
.superpowers/

# Runtime-generated Xray config (contains per-session VLESS credentials;
# regenerated by the server on every startup)
xray_config.json

# Compiled test binaries (e.g. store.test from `go test -c`)
*.test

# Internal planning scratch space (never commit)
.scratch/

# Tests and coverage
*.out
coverage.html

# Misc
*.swp
*.swo
*~
.DS_Store
*.log
config/local.yaml
config.yaml
.env
.idea/
*.iml
.claude/*
!.claude/skills/
!.claude/skills/**
!.claude/agents/
!.claude/agents/**
logs/
.gstack/

# Dev build binaries (accidental `go build` outputs)
/server
cmd/server/server
# Added by code-review-graph
.code-review-graph/

# code-review-graph tooling artifacts (tool-managed, regenerable)
.github/code-review-graph.instruction.md

# Editor-local config
.vscode/

# AI tool mirrors were removed 2026-07-28 (GEMINI.md / CODEBUDDY.md /
# .cursorrules / .windsurfrules / .codebuddy/ / .gemini/ / .kiro/ / .qoder/).
# AGENTS.md is the single master and IS tracked; do not recreate mirrors
# or re-add ignore entries that hide them. See AGENTS.md header.

# Release rehearsal local config (SSH target privacy) - never commit
scripts/release/rehearsal.local.conf
__pycache__/
