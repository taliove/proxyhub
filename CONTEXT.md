# ProxyHub

代理订阅聚合系统:从多个上游机场拉取节点,健康检查与过滤后,聚合成对终端设备暴露的订阅地址。本词汇表统一团队用语,消除"订阅"一词的方向歧义。

## Language

**机场 (Airport)**:
上游订阅提供方。ProxyHub 从其订阅 URL 拉取节点。部分机场需在其面板手动开权限后订阅 URL 才可用。
_Avoid_: 上游、供应商、订阅源

**手动机场 (Manual Airport)**:
数据源不是订阅 URL、而是由用户直接粘贴订阅导出内容的机场。定位:有些机场禁止外网访问/拉取其订阅地址,只能在其面板手动导出节点列表,粘贴导入是这类机场唯一的接入方式。创建与更新都是显式粘贴动作(粘贴即触发一次该机场的单机场 upsert,语义同单机场刷新);系统不保存粘贴原文;定时刷新与全量刷新自动跳过手动机场。粘贴内容自动识别两种形态:base64 整段编码(机场面板标准导出)与明文多行分享链接;部分行解析失败不阻断——成功行入池,失败行逐行报告(与拉取的 ParseWithStats 语义一致)。机场测试时 URL 拉取诊断标 N/A,评分按"URL 不可达且池有节点"权重重归一(现成语义)。不参与周期健康检查:节点不在全量刷新的拉取集内,可用性靠检测状态保留(per-source 合并)与手动检测(检查动作/机场测试)维持。机场节点清空豁免手动机场节点(无 URL 可拉,清空后永不回来)。
_Avoid_: 离线机场、本地机场
_区别_: 自建节点是 FailBack 语义的单节点,豁免一切订阅时过滤;手动机场节点是机场节点,走完整的地区识别、名称标准化与三道过滤链。

**用量信息 (Usage Info)**:
机场的流量与有效期元数据:已用流量(上行+下行)、总流量、过期时间、官网地址。拉取型机场在每次订阅拉取时从响应头自动捕获(`subscription-userinfo` 的 upload/download/total/expire;官网取 `profile-web-page-url`);手动机场无响应头,由用户在粘贴导入时按引导手填,全部可选、留空即不展示。用途仅限展示(机场列表与详情,临期/用尽标红),不触发告警、不自动停用(告警联动见 spec-manual-airport-import 拆分暂存)。
_Avoid_: 配额(配额是 ProxyHub 对普通用户的资源上限,见"资源配额";用量是机场侧的套餐余量)

**订阅地址 (Endpoint)**:
ProxyHub 对终端设备暴露的出站订阅 URL(随机 path + token,公开访问、无需登录)。
_Avoid_: 裸用"订阅"、链接

> ⚠️ **"订阅"是重载词**:"机场订阅"= 入站(我们从机场拉),"订阅地址"= 出站(终端从我们拉)。单说"订阅"必须指明方向。

**分享链接 (Share Link)**:
单个节点的 URI(`vmess://`/`vless://`/`trojan://`/`ss://`/`anytls://`),供客户端导入单节点使用。share-uri 端点优先回放解析期保留的机场原始链接(`Node.RawLink`,含凭证故 `json:"-"`,不入视图/日志/持久化);缺失时(自建节点、重启后未刷新)回退为按节点字段重建,重建不保证 ws path/Reality 等参数保真。
_区别_: 订阅地址是多节点聚合的出站端点;分享链接是单节点 URI。

**节点 (Node)**:
单个代理服务器(VMess / VLESS / Trojan / Shadowsocks)。

**聚合 (Aggregation)**:
拉取机场 → 健康检查 → 过滤 → 更新节点池 的流水线。产出供订阅地址生成用的节点池。
_Avoid_: 刷新(刷新是触发聚合的动作,不是聚合本身)

**刷新 (Refresh)**:
触发一轮聚合的动作。按触发源分三种:定时刷新、手动刷新、启动刷新;按范围分两种:**全量刷新**(拉取所有启用机场,含健康检查)与**单机场刷新**(只拉取一个机场并 upsert 入池,不跑健康检查,用于刚加机场/换订阅 token 后快速可见)。刷新统一执行为任务(job kind),进任务中心;同一机场同时最多一个刷新任务(机场级互斥),不同机场的单机场刷新可并行,全量刷新与任何单机场刷新互斥。多机场拉取并行,并行度为系统设置项(默认 4)。
_Avoid_: 把"单机场刷新"理解为"对单机场的全量流水线"(不含健康检查)

**健康检查 (Health Check)**:
两阶段判定节点可用性:延迟测速 + 真实请求测试(见 ADR 0002)。

**机场测试 (Airport Test)**:
针对单个机场手动触发的整体评价流水线,采用池优先策略:拉取诊断(非阻断:HTTP 状态/耗时/节点数/解析失败行数)→ 条件分支(池有该机场节点→直接测试;池无→URL 通则 upsert 入池再测试,URL 不通则 failed)→ 分层抽样检活(地区分层,HK/SG/US 优先区每层最多 5 个、其余每层最多 2 个,层内随机,结果写回节点可用状态;可升级全量)→ 综合评分(URL 可达:可用率 50% + 延迟 30% + 拉取健康 10% + 地区覆盖 10%;URL 不可达且池有节点:权重重归一为可用率 5/9 + 延迟 3/9 + 地区 1/9,拉取健康标 N/A)。结果落 `airport_test_runs` 表,机场列表展示最近得分。诊断失败不阻断测试(池有节点继续测试),只在池空 + URL 不通时 failed。
_执行形态_: 标准 job kind(`airport_test`,key=`airport-<id>`,见 ADR 0027):任务中心可见、可取消(run 标 cancelled,已写回检活结果不回滚)、崩溃标 interrupted;与同机场刷新互斥(任一在跑,另一个发起即 409);进度以 jobs cursor 为主(`{phase,checked,total}`),sample_params 为镜像。
_呈现_: 报告主体是事实汇总(可用 x/y、平均延迟、地区覆盖)+ 抽样节点明细表;评分旁直接拆解维度构成(打开黑盒,不让人猜"鬼分数")。查看历史报告不触发重跑,重跑是显式动作(见"详情抽屉")。

**订阅测试 (Endpoint Test)**:
针对单个订阅地址手动触发的即时诊断,不落库、看完即走。三段:拉取验证(走内部生成链而非真实 HTTP,验证内容合法性与下发节点数,不记 `pull_logs`、不污染拉取统计)→ 下发节点池状态快照(可用 x/y、平均延迟、地区覆盖)→ 可选现场实测(地区分层抽样检活,可升级全量,与机场测试同一套机制)。订阅地址禁用态也可测(测"如果启用会下发什么")。订阅列表的"可用 x/y"直接从池状态实时算,不依赖测试记录(见 ADR 0028)。
_Avoid_: 订阅体检(体检是节点级深度检测,与订阅地址无关)

**详情抽屉 (Detail Drawer)**:
机场与订阅地址的统一查看入口(点行内「详情」打开),定位是用户遍览与轻管理。机场抽屉三段:概况+轻管理动作 → 池内节点明细 → 最近测试报告;订阅抽屉四段:概况+轻管理动作 → 下发节点清单(订阅原文折叠)→ 订阅测试 → 拉取统计。抽屉内节点行只允许轻动作(复制分享链接、触发体检);屏蔽等状态变更归节点管理页。抽屉内查看历史测试结果不触发重跑。行内按钮随之极简:机场行留「详情」+「刷新」,订阅行留「详情」。

**关键词黑名单 (Keyword Blacklist)**:
系统设置里的一组名称关键词。订阅生成时,名称命中任一关键词的**机场节点**被剔除;自建节点豁免。子串匹配、不区分大小写(见 ADR 0005)。

**关键词白名单 (Keyword Whitelist)**:
系统设置里的一组名称关键词。非空时,只有名称命中任一关键词的**机场节点**才保留,其余剔除;为空则不启用(保留全部)。自建节点豁免。与黑名单同套机制(子串、不区分大小写、订阅时生效),方向相反(见 ADR 0009)。
_注_: 这是字符串匹配层面的粗粒度过滤;精细化的地区筛选与映射见"地区识别""分区占位符"。

**机场节点屏蔽 (Node Block)**:
按节点唯一标识(`server:port`,即 NodeKey)精确拉黑单个机场节点的持久名单。跨刷新保留(不像手动删节点下轮被冲回)。订阅生成时命中即剔除;自建节点豁免(见 ADR 0009)。
_Avoid_: 删除节点(机场节点删了下轮刷新会冲回来,"屏蔽"才是持久的剔除语义;一次性重置见"机场节点清空")

**任务结果 (Job Result)**:
任务详情按 kind 派生展示的任务产出,经 `GET /api/jobs/{id}/result` 按 kind 分发。关联规则:refresh → refresh_runs(job_id 反查,见 ADR 0026);exam/batch_exam → exam_history(job_id 列为主路径,无 job_id 的老数据回退任务时间窗派生并标注"非本次任务产出");airport_test → airport_test_runs(job_id 列,见 ADR 0027)。任务中心支持 `/jobs?id=<jobId>` URL 直达详情。
_区别_: 任务结果是"这次跑出了什么"(报告),不同于任务状态/进度(跑没跑完)。

**直连出口 (Direct Egress)**:
检测链路的拨号出口控制。开启时(默认开),健康检查/真实检测/体检/解锁/带宽等到节点服务器的连接:节点域名经自带 DoH 解析为真实 IP(绕过系统 DNS 的 fake-ip 劫持),且套接字绑定物理网卡(绕过 TUN 接管的默认路由);绑定失败时检测报错而非悄悄退化(严格模式)。定位:保证"ProxyHub 跑在开着 TUN 代理客户端的机器上时,检测结果仍是节点对宿主机的真实表现"。只作用于检测链路,机场订阅拉取不适用(见 spec-detection-direct-egress 拆分暂存)。
_Avoid_: 绕过代理(它绕的是本机劫持,不是"用代理访问节点")

**机场节点清空 (Pool Purge)**:
一次性从节点池(内存池 + DB 快照双清)删除全部机场节点的手动动作。自建节点豁免;屏蔽名单、名称覆盖、自动标签保留,清空后节点随下轮刷新重新入池。定位是排障用的"重置起点",不是持久剔除语义(持久剔除用"机场节点屏蔽");清空后不自动刷新,由用户手动触发。
_区别_: 屏蔽 = 持久名单、节点留池、订阅时剔除;清空 = 一次性删池、下轮刷新冲回。

**节点过滤顺序**:
订阅生成时三道过滤依次执行:白名单(要哪些)→ 黑名单(再排除哪些)→ 机场屏蔽(精确点杀);自建节点全程豁免(见 ADR 0009)。

**自建节点 (Self-hosted Node)**:
用户自己常驻的节点,作为 FailBack 注入节点池,不参与机场拉取与告警,豁免所有订阅时过滤。支持增/改/删/启停(见 ADR 0009)。
_Avoid_: 备用节点

**配置模板 (Template)**:
Clash 订阅的配置骨架,含 hosts/dns/proxy-groups/rules。节点通过占位符动态注入。平台提供内嵌默认模板(含常见地区分组)。模板按回退链解析:订阅地址选定模板 ?? 用户默认模板(见"模板库") ?? 超管全局默认 ?? 内嵌默认(见 ADR 0006)。
_Avoid_: 配置文件、profile

**模板库 (Template Library)**:
某一用户名下的一套命名配置模板集合(每套 = 名称 + 内容),其中一套被标记为该用户的**默认模板**。订阅地址创建/编辑时可从库中挑选一套模板;不挑则跟随默认模板。模板对订阅地址是软引用:删除一套被引用的模板时,引用它的订阅地址自动回退到默认模板。库归用户所有,随用户空间隔离,超管全局默认不在库内、只做链底兜底。

**模板版本 (Template Version)**:
模板库成员每次成功落库产生的一个历史版本(版本号递增,含内容与落库时间),每模板保留最近 20 个,超出裁剪最旧;模板删除时其版本一并删除。历史只追加不可变。
_Avoid_: 草稿、快照(快照语义是手动触发,版本是落库的自然产物)

**校验门控自动保存 (Validated Auto-save)**:
模板编辑器的落库策略:编辑停止后防抖自动保存,但仅在 YAML 校验通过时才真正落库;校验不通过只本地标红、不落库。不改变"落库即对下一次订阅生效"的语义——坏配置永远不会流给订阅方。副作用:非法内容切换模板时丢失(切换前有确认)。

**回滚 (Rollback)**:
从模板版本历史中选定一个旧版本,预览确认后以其内容重新落库。回滚不产生破坏性操作:恢复 = 一次新的成功落库,自身也成为一个新的模板版本,历史不删减。预览态以并排 diff(历史版本 vs 当前编辑器内容)呈现差异。

**模板重置 (Template Reset)**:
把模板库中某套模板的内容恢复为平台内嵌默认模板的手动动作,经确认对话框后落库。与回滚同语义:重置 = 一次新的成功落库,自身成为一个新的模板版本。区别于全局配置模板的 `settings/template/reset`(那是超管全局默认链上的另一套)。

**占位符 (Placeholder)**:
模板中的 `{{nodes}}` 标记,出现在某个 proxy-group 的 proxies 数组里。订阅生成时被替换为当前节点池的全部节点名(见 ADR 0007)。

**地区识别 (Region Recognition)**:
从机场拉取的节点名("XX 香港"、"HK-01"、"🇭🇰 高速")通过地区规则库提取标准地区代码(HK/SG/US/JP 等)。识别结果存入 `nodes.region` 字段,供订阅生成时按地区过滤与分组。规则库覆盖中英文名、国旗 emoji、常见缩写(参考 subconverter 地区库)。
_Avoid_: 地区映射(映射侧重改名,识别侧重分类)

**地区白名单 (Region Whitelist)**:
系统设置里的地区代码列表(如 `[HK, SG, US]`)。非空时,订阅生成时只下发白名单地区的节点;为空则下发全部。作用于订阅生成阶段(节点仍全部入池,只是部分地区不下发给终端)。
_区别_: 与"关键词白名单"不同,这是按地区代码精确筛选,而非字符串子串匹配。

**机场简称 (Airport Abbreviation)**:
机场名称的短标识符(如"极速机场" → "JS")。用于节点名称标准化时构造简洁的节点名。自动生成规则:中文取拼音首字母大写,英文取前2-3个大写字母;重复时加数字后缀(JS2, JS3)。存储在 `airports.abbr` 字段,支持管理员手动覆盖自动生成的简称(见 ADR 0012)。
_Avoid_: 机场代码、机场标识

**节点名称标准化 (Node Name Standardization)**:
订阅生成时将机场原名("XX 香港"、"🇭🇰 高速节点")转换为统一格式的过程。标准格式由模板定义(如 `{emoji} {region} {source_abbr}-{index}` → "🇭🇰 香港 JS-01")。转换仅影响下发给终端的订阅内容,`nodes.name` 字段永远保留机场原名,`nodes.display_name` 字段缓存标准化名称(见 ADR 0012)。
_Avoid_: 节点名映射、节点重命名

**名称模板 (Name Template)**:
订阅端点配置中定义节点标准化格式的模板字符串。支持占位符: `{emoji}`(地区国旗)、`{region}`(地区中文名)、`{region_code}`(地区代码)、`{source}`(机场全名)、`{source_abbr}`(机场简称)、`{index}`(序号)、`{original_name}`(原始名)。例: `"{emoji} {region} {source_abbr}-{index}"` 生成 "🇭🇰 香港 JS-01"(见 ADR 0012)。
_Avoid_: 格式化字符串、节点模板

**分区占位符 (Region Placeholder)**:
模板中的增强占位符,支持按地区过滤注入。语法: `{{nodes:region=HK}}`(单地区) 或 `{{nodes:region=HK,SG,US}}`(多地区)。生成订阅时只注入匹配地区的节点,用于构建"🇭🇰 香港节点""🇸🇬 新加坡节点"等地区分组。
_Avoid_: 裸用 `{{nodes}}` 表示注入全部地区节点。

**审计事件 (Audit Event)**:
登录相关的安全事件流水(登录成功/失败、蜜罐命中封禁、达阈值封禁、验证码失败、MFA 绑定/失败/重置;免验登录记 `login_success` 带 `mfa_skipped=trusted_ip` 标记),落 `audit_logs` 表,保留 90 天。区别于 `banned_ips`(当前封禁状态计数器):审计是历史流水,封禁表是当前状态(见 ADR 0010)。
_Avoid_: 日志(与 slog 应用日志区分)

**多因素认证 (MFA)**:
管理面全部账号(超管 + 普通用户)强制绑定的 TOTP(RFC 6238)第二因素;复刻 `requirePasswordChanged` 模式做 `requireMFAEnrolled`,未绑定账号登录后只能进入绑定页。登录为两段式:密码通过但需 MFA 时发短效 `mfa_pending` 临时凭证,第二步提交 TOTP 码或恢复码换正式会话。MFA 失败计入登录失败计数(与密码/验证码错误同阈值同封禁)。恢复通道三层:10 个一次性恢复码(绑定时强制确认已保存)→ 超管重置普通用户 → loopback CLI 重置超管。
_区别_: 登录验证码防的是自动化脚本(人机区分);MFA 防的是凭证泄露(身份自证),两者在 `handleLogin` 共存互补。

**受信 IP (Trusted IP)**:
某账号的免 MFA 来源 IP 列表,按用户隔离,初始为空。建立:仅显式决策——MFA 成功页勾选"信任此 IP 30 天",或采纳信任推荐(系统统计每 IP 成功 MFA 登录达阈值后推荐,不静默自动信任;另有默认关闭的"自动信任模式"可选)。失效:30 天过期,受信 IP 每次成功登录自动续期;设置页可单条撤销,超管可清空任意用户列表。loopback 不自动受信(可显式信任)。免验登录的审计记 `login_success` 带 `mfa_skipped=trusted_ip` 标记。

**登录验证码 (Login Captcha)**:
管理面登录的渐进式人机校验,定位是 IP2Ban 之前的软拦截层(封禁是硬拦截:拒绝服务;验证码是软拦截:增加自动化成本但不误伤人)。同一 IP 登录失败达可配次数(超管专属设置 `captcha_trigger_threshold`,默认 1,判定依据 `banned_ips.fail_count`)后,后续登录必须携带自托管图形验证码:6 位字母数字(剔除易混淆字符 0/O/1/I/l),噪点+干扰线+旋转扭曲,TTL 5 分钟,一次性(提交即作废,对错都算)。由 401 响应体 `captcha_required` 驱动前端渲染,无独立探测接口;验证码错误计入登录失败计数(与密码错误同阈值触发 IP2Ban),审计事件 `captcha_failure`。仅作用于 `POST /api/login`,loopback 豁免,Setup 初始化页不覆盖。

**体检 (Node Exam)**:
针对单个节点手动或批量触发的深度检测流水线,四段按执行顺序串行(各段独占节点会话不重叠,见 internal/detection/exam.go `ExamOrchestrator`):出网质量(IPv4/IPv6/DNS,前置以便 ~5s 内看到出口并对死节点 fail-fast,全失败即短路后续段)→ 稳定性(丢包/抖动/P95 延迟加权出 0..100 分,P95 延迟分量权重 0.25)→ 区域速率(基准 + 8 区)→ 流媒体/AI 解锁。四段加权出总分(稳定性 40% + 速度 25% + 解锁 20% + 出网 15%,分档 90+ 极好/75+ 良好/60+ 一般/40+ 较差),结果落 `exam_history` 表(见 ADR 0015/0022)。
_区别_: 机场测试是机场级抽样评分(ADR 0024);体检是节点级深度检测。

**基准行 (Baseline)**:
体检"区域速率"段的第 1 行对照行(展示名"基准",code=`baseline`):经被测节点打 Cloudflare 就近 Anycast POP(`speed.cloudflare.com` 100MB 文件),近似该节点自身带宽上限的参考值。速度评分(对数映射)与 `fast` 标签门槛均以基准行下行为准;批量精简体检只测基准行、跳过 8 个真实区域(见 docs/design-node-exam.md)。
_Avoid_: 把"基准"理解为基准地区或独立对照组实验

**检查动作 (Check Actions)**:
节点页统一的四个手动检查动作,批量(作用于勾选集)与单节点(行内)共用同一套词汇:
1) 出网快速检测:TCP 快筛 + 延迟 + 真实请求 + 配置的解锁目标;
2) 出网+稳定性:两阶段出网(不含解锁目标)+ 体检稳定性段(含 0-100 评分);
3) 快速测速:基准下行(见"基准行"口径,单点);
4) 深度体检:完整四段体检(见"体检")的任务化派发,批量作用于勾选集、单节点作用于本行(共用 `batch_exam` mode=full)。
_Avoid_: 快测/真实检测/带宽测试/检测全部/检测筛选结果/批量体检等旧入口叫法(收敛后废弃,全站只有这四个动作词汇)

**本机实测 (Client Speedtest)**:
浏览器发起、后端执行的实测:用户在页面选择节点,浏览器调用 ProxyHub API,后端通过该节点代理访问公共测速端点(Cloudflare speed test),测量"用户浏览器 ↔ ProxyHub ↔ 节点 ↔ 测速端点"全链路的下行/上行/空闲延迟/抖动。测得速度 = 用户到节点的实际可用带宽(受 ProxyHub 带宽上限约束,但反映真实使用体验)。支持"直连"模式(ProxyHub 不经节点直接访问测速端点),作为对比基线。结果落库,历史按节点聚合并与直连基线做差值对比(节点开销 = 经节点实测 - 直连实测)。标注节点已不在池的历史记录标"已失效"保留,不级联删;节点回池后"已失效"自动消失。
_区别_: 检查动作 3"快速测速"是 ProxyHub 主机经节点的筛选性测量(可批量、入库、派生标签);本机实测是浏览器端当下真实链路的验收性测量(单次、当下、只对这台设备有意义)。

**自动标签 (Node Tag)**:
从节点检测结果派生的语义标签(如 `nf-full`、`fast`、`stable-good`、`region:JP`),由纯函数从最新解锁判定 + 最近体检报告派生,落 `node_tags` 表持久化,供订阅筛选与列表展示。重算时机:批量解锁检测后、每场体检后、节点上下线时、夜间兜底(见 ADR 0023)。

**优质节点 (Top Node)**:
主页展示的体检总分最高节点榜单(Top 10)。只收录体检过的节点,且要求当前在节点池内(推荐"现在就能用"的节点);每条带分享链接复制与二维码。

**异常面板 (Alert Panel)**:
主页的异常聚合区:低分机场(最近机场测试总分 <60)、24h 内失败/中断任务、当前被封禁 IP、24h 审计异常事件;全部为空时显示"一切正常"。只做聚合展示与跳转,不含未读/告警中心机制。

**拉取统计 (Pull Stats)**:
订阅地址的访问统计,基于 `pull_logs`(每次 /sub 请求记一行,含 `status`:ok / rate_limited / geo_blocked / geo_would_block / blacklisted / disabled / bad_token——被防护链拦截的请求同样留痕;503/500 等服务端态不留)。分全局汇总(总拉取/独立 IP/活跃订阅)、趋势(按天分订阅地址)、单地址 IP 明细(次数/状态/最后拉取/地理)三个视图(见 ADR 0010)。**聚合口径**:全局汇总与趋势只统计 `status='ok'` 的真实客户端拉取,扫描探测不灌爆统计;明细保留全部状态(见 ADR 0033)。

**拉取防护 (Pull Guard)**:
对订阅地址(/sub)公开入口的三道守卫,全部在 path+token 验证通过之后由守卫链骨架统一执行(无效 token 永远只得统一 404;429/403 只发给持有效 token 的请求;骨架代理留痕与响应),链序:① **拉取黑名单**:命中 `ip_access_rules` 的 scope=sub 规则回 403;同一 IP 1 小时内被限频达 10 次(可配)自动写入 source=auto、默认 24h(可配)的规则,超管可升级为 global;② **地域白名单**(见专条);③ **拉取限频**:IP×订阅地址内存滑动窗口(默认 60 次/小时,0=关闭,超管设置),超限回 429+Retry-After。与登录侧 `banned_ips` 体系相互独立(见 ADR 0033)。
_Avoid_: 与登录 IP2Ban 混为一谈(两套独立体系,不同表、不同配置、不同入口)

**地域白名单 (Geo Allowlist)**:
订阅地址级的拉取来源地域限制,在订阅详情抽屉按地址配置,三档:关闭(默认)/ 观察(判定不拦截,记 `geo_would_block`)/ 拦截(未命中 403 记 `geo_blocked`)。配置 = 国家列表 + 省份列表;**空列表 = 该维度不判**;判定不到位置的来源(查询失败/私网/无数据)在拦截档一律视为未命中(保守)。判定走内嵌 GeoIP 离线查询——当前库为 Country 级、**省级数据为零,省份配置永不命中**(数据门控:换 City 级库即生效,UI 已折叠警告)。观察档用于先验证 GeoIP 对自己设备的判定准确率(蜂窝网出口常落网关省),确认无误伤再升拦截档。
_区别_: 关键词/地区白名单过滤的是下发哪些节点;地域白名单过滤的是谁来拉取。

**整站 IP 黑名单 (Global IP Blacklist)**:
统一规则表 `ip_access_rules` 中 scope=global 的规则,由挂在 Site Path 中间件之后、路由分发之前的拒止中间件执行,命中即统一 404(与 Site Path 未命中逐字节一致,不暴露任何存在性);store 查询失败 fail-open;loopback 永远豁免(SSH 隧道是逃生门)。拉取防护的自动黑名单条目为 scope=sub(只拒 /sub),超管可在审计页"IP 规则"区块一键升级为 global;手动条目建时选定 scope。纯黑名单模型——管理面白名单已否决(自锁风险大于收益,Site Path + loopback 监听 + Caddy 层白名单三道既有防线充分,否决记录见 ADR 0033)。
_区别_: 登录 IP2Ban 是带失败计数的自动处置(handleLogin 内,独立表);整站黑名单是显式规则 + 全路由中间件。

**Caddy 模式 (Caddy Mode)**:
一键安装器对目标机 Caddy 拓扑的识别结果,三态:`native`(宿主机原生 systemd Caddy,`command -v caddy` 探测)/ `docker`(容器化 Caddy,见"Docker Caddy 模式")/ `none`(`--no-caddy`,运维自带反代,安装器只写示例文件)。记录在安装档案的 `CADDY_MODE` 字段,proxyhubctl 的 update/repair/rotate-path/uninstall 读档案后走对应通道;旧档案缺省按 `native` 兼容。模式优先级:`--caddy-docker` 强制 docker > 有 caddy 二进制走 native > 自动探测单个 docker caddy 容器 > fail closed(见 ADR 0035)。
_Avoid_: 反代模式、部署模式

**Docker Caddy 模式 (Docker Caddy Mode)**:
Caddy 模式的一种:目标机的 Caddy v2 跑在 Docker 容器里,安装器识别并集成它(ProxyHub 本体仍是 systemd 裸机安装)。容器选择混合制:`--caddy-docker <容器名>` 显式指定,不指定时恰好一个运行中的 caddy 镜像容器自动选用并明示,零个/多个 fail closed。配置投递解析容器 `/etc/caddy` 持久挂载(bind mount 或 named volume,经宿主机路径写 fragment、追加容器路径语义的 import 行;单文件挂载等形态 fail closed)。回连拓扑自动识别:`network_mode: host` 容器零改动(loopback 原样);桥接容器绑定该网桥网关 IP、`trusted_proxies` 收窄到网桥子网、fragment 用 `host.docker.internal`(校验 host-gateway 映射)。**安全降级有界且明示**:桥接模式下管理面信任边界从 loopback 扩到该 docker 网桥——同网桥任意容器可直连管理面(无 TLS)且可伪造 XFF;桥接上回连流量为明文 HTTP,Site Path 可被网桥内嗅探者被动获取(Site Path 保密对网桥对端不成立),XFF 替换仍在(见 ADR 0035)。
_Avoid_: 容器部署(ProxyHub 本体仍裸机,不是全容器化编排)

**用户 (User)**:
ProxyHub 管理面的账号实体,分超级管理员(super_admin)与普通用户(user)两种角色。普通用户拥有独立的机场、节点池、订阅地址、自建节点与 Xray 实例,资源互相隔离。由超管在后台开通,初始密码 + 首登强制改密。

**资源配额 (User Quota)**:
对普通用户的资源上限约束,包括机场数量、订阅地址数量、Xray 端口范围。超管在创建/编辑用户时分配,超限操作被系统拒绝。

**用户空间 (User Space)**:
某一用户可见与操作的资源集合(机场/节点/订阅地址/自建节点/Xray 实例)。普通用户只能访问自己的用户空间;超管可切换查看/修改任意用户空间。

**租户级设置 (Per-user Setting)**:
按用户存取的配置键(关键词黑白名单/地区白名单/名称标准化与名称模板/配置模板/解锁检测目标/定时刷新开关),落 `user_settings` 表,只作用于该用户的用户空间(订阅过滤链/订阅生成/检测)。读取回退链:用户覆盖值 ?? 全局默认(`system_settings`) ?? 内置默认;"重置"即删除覆盖行、回到跟随全局默认,超管调全局默认时未覆盖的用户自动跟随。与"超管专属设置"(封禁策略/直连出口/拉取并发/测速体检技术参数/告警/标签重算调度,只走 `system_settings` 且接口需超管)相对。
_区别_: 用户空间是数据资源的隔离边界;租户级设置是配置的隔离边界,两者都按 EffectiveUserID 生效。
