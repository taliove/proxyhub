# ProxyHub

代理订阅聚合系统:从多个上游机场拉取节点,健康检查与过滤后,聚合成对终端设备暴露的订阅地址。本词汇表统一团队用语,消除"订阅"一词的方向歧义。

## Language

**机场 (Airport)**:
上游订阅提供方。ProxyHub 从其订阅 URL 拉取节点。部分机场需在其面板手动开权限后订阅 URL 才可用。
_Avoid_: 上游、供应商、订阅源

**订阅地址 (Endpoint)**:
ProxyHub 对终端设备暴露的出站订阅 URL(随机 path + token,公开访问、无需登录)。
_Avoid_: 裸用"订阅"、链接

> ⚠️ **"订阅"是重载词**:"机场订阅"= 入站(我们从机场拉),"订阅地址"= 出站(终端从我们拉)。单说"订阅"必须指明方向。

**分享链接 (Share Link)**:
单个节点的 URI(`vmess://`/`vless://`/`trojan://`/`ss://`/`anytls://`),供客户端导入单节点使用。share-uri 端点优先回放解析期保留的机场原始链接(`Node.RawLink`,含凭证故 `json:"-"`,不入视图/日志/持久化);缺失时(自建节点、重启后未刷新)回退为按节点字段重建,重建不保证 ws path/Reality 等参数保真。
_区别_: 订阅地址是多节点聚合的出站端点;分享链接是单节点 URI。

**节点 (Node)**:
单个代理服务器(VMess / VLESS / Trojan / Shadowsocks)。

**聚合 (Aggregation)**:
拉取机场 → 健康检查 → 过滤 → 更新节点池 的流水线。产出供订阅地址生成用的节点池。
_Avoid_: 刷新(刷新是触发聚合的动作,不是聚合本身)

**刷新 (Refresh)**:
触发一轮聚合的动作。按触发源分三种:定时刷新、手动刷新、启动刷新;按范围分两种:**全量刷新**(拉取所有启用机场,含健康检查)与**单机场刷新**(只拉取一个机场并 upsert 入池,不跑健康检查,用于刚加机场/换订阅 token 后快速可见)。刷新统一执行为任务(job kind),进任务中心;同一机场同时最多一个刷新任务(机场级互斥),不同机场的单机场刷新可并行,全量刷新与任何单机场刷新互斥。多机场拉取并行,并行度为系统设置项(默认 4)。
_Avoid_: 把"单机场刷新"理解为"对单机场的全量流水线"(不含健康检查)

**健康检查 (Health Check)**:
两阶段判定节点可用性:延迟测速 + 真实请求测试(见 ADR 0002)。

**机场测试 (Airport Test)**:
针对单个机场手动触发的整体评价流水线,采用池优先策略:拉取诊断(非阻断:HTTP 状态/耗时/节点数/解析失败行数)→ 条件分支(池有该机场节点→直接测试;池无→URL 通则 upsert 入池再测试,URL 不通则 failed)→ 分层抽样检活(地区分层,HK/SG/US 优先区每层最多 5 个、其余每层最多 2 个,层内随机,结果写回节点可用状态;可升级全量)→ 综合评分(URL 可达:可用率 50% + 延迟 30% + 拉取健康 10% + 地区覆盖 10%;URL 不可达且池有节点:权重重归一为可用率 5/9 + 延迟 3/9 + 地区 1/9,拉取健康标 N/A)。结果落 `airport_test_runs` 表,机场列表展示最近得分。诊断失败不阻断测试(池有节点继续测试),只在池空 + URL 不通时 failed。

**关键词黑名单 (Keyword Blacklist)**:
系统设置里的一组名称关键词。订阅生成时,名称命中任一关键词的**机场节点**被剔除;自建节点豁免。子串匹配、不区分大小写(见 ADR 0005)。

**关键词白名单 (Keyword Whitelist)**:
系统设置里的一组名称关键词。非空时,只有名称命中任一关键词的**机场节点**才保留,其余剔除;为空则不启用(保留全部)。自建节点豁免。与黑名单同套机制(子串、不区分大小写、订阅时生效),方向相反(见 ADR 0009)。
_注_: 这是字符串匹配层面的粗粒度过滤;精细化的地区筛选与映射见"地区识别""分区占位符"。

**机场节点屏蔽 (Node Block)**:
按节点唯一标识(`server:port`,即 NodeKey)精确拉黑单个机场节点的持久名单。跨刷新保留(不像手动删节点下轮被冲回)。订阅生成时命中即剔除;自建节点豁免(见 ADR 0009)。
_Avoid_: 删除节点(机场节点删了下轮回来,"屏蔽"才是正确语义)

**节点过滤顺序**:
订阅生成时三道过滤依次执行:白名单(要哪些)→ 黑名单(再排除哪些)→ 机场屏蔽(精确点杀);自建节点全程豁免(见 ADR 0009)。

**自建节点 (Self-hosted Node)**:
用户自己常驻的节点,作为 FailBack 注入节点池,不参与机场拉取与告警,豁免所有订阅时过滤。支持增/改/删/启停(见 ADR 0009)。
_Avoid_: 备用节点

**配置模板 (Template)**:
Clash 订阅的配置骨架,含 hosts/dns/proxy-groups/rules。全局唯一、所有订阅地址共享,可在后台编辑;节点通过占位符动态注入。平台提供默认模板(含常见地区分组),用户可编辑或一键恢复默认(见 ADR 0006)。
_Avoid_: 配置文件、profile

**占位符 (Placeholder)**:
模板中的 `{{nodes}}` 标记,出现在某个 proxy-group 的 proxies 数组里。订阅生成时被替换为当前节点池的全部节点名(见 ADR 0007)。

**地区识别 (Region Recognition)**:
从机场拉取的节点名("XX 香港"、"HK-01"、"🇭🇰 高速")通过地区规则库提取标准地区代码(HK/SG/US/JP 等)。识别结果存入 `nodes.region` 字段,供订阅生成时按地区过滤与分组。规则库覆盖中英文名、国旗 emoji、常见缩写(参考 subconverter 地区库)。
_Avoid_: 地区映射(映射侧重改名,识别侧重分类)

**地区白名单 (Region Whitelist)**:
系统设置里的地区代码列表(如 `[HK, SG, US]`)。非空时,订阅生成时只下发白名单地区的节点;为空则下发全部。作用于订阅生成阶段(节点仍全部入池,只是部分地区不下发给终端)。
_区别_: 与"关键词白名单"不同,这是按地区代码精确筛选,而非字符串子串匹配。

**机场简称 (Airport Abbreviation)**:
机场名称的短标识符(如"极速机场" → "JS")。用于节点名称标准化时构造简洁的节点名。自动生成规则:中文取拼音首字母大写,英文取前2-3个大写字母;重复时加数字后缀(JS2, JS3)。存储在 `airports.abbr` 字段,支持管理员手动覆盖自动生成的简称(见 ADR 0012)。
_Avoid_: 机场代码、机场标识

**节点名称标准化 (Node Name Standardization)**:
订阅生成时将机场原名("XX 香港"、"🇭🇰 高速节点")转换为统一格式的过程。标准格式由模板定义(如 `{emoji} {region} {source_abbr}-{index}` → "🇭🇰 香港 JS-01")。转换仅影响下发给终端的订阅内容,`nodes.name` 字段永远保留机场原名,`nodes.display_name` 字段缓存标准化名称(见 ADR 0012)。
_Avoid_: 节点名映射、节点重命名

**名称模板 (Name Template)**:
订阅端点配置中定义节点标准化格式的模板字符串。支持占位符: `{emoji}`(地区国旗)、`{region}`(地区中文名)、`{region_code}`(地区代码)、`{source}`(机场全名)、`{source_abbr}`(机场简称)、`{index}`(序号)、`{original_name}`(原始名)。例: `"{emoji} {region} {source_abbr}-{index}"` 生成 "🇭🇰 香港 JS-01"(见 ADR 0012)。
_Avoid_: 格式化字符串、节点模板

**分区占位符 (Region Placeholder)**:
模板中的增强占位符,支持按地区过滤注入。语法: `{{nodes:region=HK}}`(单地区) 或 `{{nodes:region=HK,SG,US}}`(多地区)。生成订阅时只注入匹配地区的节点,用于构建"🇭🇰 香港节点""🇸🇬 新加坡节点"等地区分组。
_Avoid_: 裸用 `{{nodes}}` 表示注入全部地区节点。

**审计事件 (Audit Event)**:
登录相关的安全事件流水(登录成功/失败、蜜罐命中封禁、达阈值封禁),落 `audit_logs` 表,保留 90 天。区别于 `banned_ips`(当前封禁状态计数器):审计是历史流水,封禁表是当前状态(见 ADR 0010)。
_Avoid_: 日志(与 slog 应用日志区分)

**拉取统计 (Pull Stats)**:
订阅地址的访问统计,基于 `pull_logs`(每次 /sub 拉取记一行)。分全局汇总(总拉取/独立 IP/活跃订阅)、趋势(按天分订阅地址)、单地址 IP 明细(次数/最后拉取/地理)三个视图(见 ADR 0010)。
