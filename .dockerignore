.git
.github
.claude
.gstack
.scratch
.idea
.vscode
.env
.env.*

# Dependencies and frontend build output (rebuilt inside the image)
node_modules
web/node_modules
web/dist
cmd/server/web

# Build artifacts
dist
proxyhub
server
.test

# GeoIP download cache (the staged mmdb under internal/geoip IS needed for
# go:embed and must stay in the build context).
.geoip-cache

# Runtime data
logs
*.db
*.db-*
*.log
*.out
config.yaml
config/local.yaml
xray_config.json

# Docs not needed in the build context
docs

# Local runtime state (dev data, logs, pid)
var
